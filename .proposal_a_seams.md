# Proposal A — Deletion Seam Map (working note, delete before PR)

Goal: keep single-pool core (one allocation, N steps, leader+sidecar, ctx.peers/announce/
shared_dir, replica sets, on_failure kill/continue, wait_all, local-mode). Remove the rest.

## DROP set → re-added per fable_plan.md (F1..F5)
- F1 lifecycle: on_failure="restart"/"callback", max_restarts, callback, FailureCallback
- F2 ports: @task(ports=), ctx.my_ports, ctx.reserve_port, PortValue, "auto" binding
- F3 placement+nodes: Pool.node_labels, Peer.on_node/on_nodes, NodeRef, ctx.nodes/ctx.node,
  node_info.py, registry node section consumed by ctx.nodes
- F4 multi-pool hetjob: Topology with >1 Pool, --het-group, component_index, per-component
  bootstrap hostname resolution
- F5 colocate_with + cycle detection

## KEEP set
- parallel(), Peer (trimmed), Pool (trimmed), Topology (single-pool), _ParallelSpec
- Peer.replicas, count/args/tasks_per_node  (replica sets KEPT)
- leader, grace_period_seconds, on_failure in {kill,continue}, announce
- ctx.peers/PeerGroup/PeerInfo, ctx.announce, wait_all, ctx.shared_dir, ctx.shutdown_requested
- registry.py (peers section + atomic writes); node section may stay as inert skeleton or be removed
- supervisor: launch, poll, kill/continue, leader shutdown, grace, local mode
- _parallel_submission.py (packaging, replica items, Job build) — minimal change

## FILE-BY-FILE

### src/slurm/parallel/types.py
- OnFailurePolicy → Literal["kill","continue"]
- DELETE: PortValue, NodeRef, FailureCallback type aliases
- Pool: remove node_labels field + its __post_init__ validation block
- Peer: remove fields max_restarts, callback, on_node, colocate_with, on_nodes
- Peer.__post_init__: drop callback/restart/on_node/on_nodes/colocate validation
- Peer.replicas(): drop max_restarts, callback, on_nodes, colocate_with params
- Topology: keep but single-pool; __post_init__ already auto-sets default_pool for 1 pool.
  Add reject if len(pools)>1 (clear "future release" error) OR keep multi but unused.
  DECISION: reject >1 pool in Topology.__post_init__ (kills validation path) AND collapse
  rendering to single component.

### src/slurm/parallel/__init__.py
- _coerce_peer / _resolve_peer_pool: drop the removed kwargs from Peer rebuild
- stale docstring NotImplementedError note can stay/trim

### src/slurm/parallel/validation.py (706 LOC)
- DELETE: _check_on_node, _check_single_node_ref, _check_colocate_with (+cycle DFS),
  callback resolvability check (675-707), node_labels checks
- KEEP: pool reference, resource capacity, replica args length, announce reserved keys,
  validate_local_capacity

### src/slurm/parallel/placement.py  → DELETE ENTIRELY (349 LOC)
- Check importers: rendering.py, topology_bootstrap.py, _parallel_submission.py

### src/slurm/parallel/node_info.py → DELETE ENTIRELY (226 LOC)
- Importers: runtime.py (ctx.nodes/node), registry.py (load_node_group)

### src/slurm/parallel/rendering.py (1094 LOC)
- _srun_flags_for_peer: drop component_index/--het-group, drop --nodelist injection
- prepare_parallel_submission/render_parallel_script: drop pool_component_index, hetjob
  divider emission; always single #SBATCH header + single component
- drop placement resolution calls

### src/slurm/parallel/plan.py (298 LOC)
- PlanPeer: drop component_index, max_restarts, callback, on_node/on_nodes
- PlanComponent: collapse/remove (single component) ; keep replica_count, leader, on_failure

### src/slurm/parallel/topology_bootstrap.py (422 LOC)
- drop per-component SLURM_JOB_NODELIST_HET_GROUP_<n> loop → single nodelist
- drop node_label pairing, on_node/on_nodes/colocate resolution
- keep: shared dir creation, peer registry skeleton (state=pending), arg files

### src/slurm/parallel/topology_supervisor.py (995 LOC)
- DELETE: _resolve_callback, _build_snapshot(for callback), _invoke_callback,
  _increment_restart_count, _preload_callbacks, _inject_nodelist
- _resolve_policy → only kill/continue (+leader handled in loop)
- run_supervisor loop: remove restart branch + callback branch; keep kill/continue/leader/grace
- _component_job_ids: collapse to single component (still cancel $JOB_ID)

### src/slurm/parallel/registry.py (635 LOC)
- Remove restart_count usage? restart_count field can stay inert (append-only schema, F1).
  Simpler: keep PeerRegistryEntry as-is minus port update fn if dropping ports.
- update_peer_ports → DELETE (F2). load_node_group/nodes_from_registry → DELETE (F3) IF
  node_info gone. node registry entries: stop writing? Keep skeleton minimal.
- KEEP: write_registry atomic, announce_peer_metadata, load_peer_groups, locks, states

### src/slurm/runtime.py (+393 diff)
- DELETE: _reserve_ephemeral_port, my_ports prop, reserve_port method (F2)
- DELETE: nodes prop, node prop (F3), _SHARED_DIR... keep shared_dir
- KEEP: peers prop, announce, shutdown_requested, shared_dir, build_job_context (trim)

### src/slurm/runner/initialization.py (625) + main.py (731)
- DELETE: resolve_peer_ports, _publish_initial_ports (F2)
- KEEP: replica by-taskid dispatch, registry path wiring, peer_name/pool/replica context

### src/slurm/task.py (+334 diff)
- DELETE: ports= kwarg in @task + its validation + propagation through with_options/after
- KEEP: BoundTask, partial, with_sidecars, replicas plumbing

### src/slurm/parallel_job.py (829)
- leader_result KEEP. peer_outcomes KEEP (statuses minus 'restarted'? keep enum, inert).
- snapshot KEEP. Drop nodes-related surface if any.

## TESTS TO DELETE (dropped features)
test_parallel_hetjob_rendering, test_hetjob_bootstrap, test_parallel_multipool_e2e,
test_placement_resolution, test_colocate_cycle, test_nodes_api, test_supervisor_restart,
test_supervisor_callback, test_parallel_callbacks (callback parts), test_ports_reservation,
test_replica_runner_dispatch? (KEEP - replicas kept)
## TESTS TO TRIM (mixed)
test_parallel_types, test_parallel_validation, test_parallel_rendering, test_parallel_plan,
test_peer_info_surface(keep), test_with_sidecars_sugar(keep), test_parallel_outcomes(trim
restarted), test_parallel_supervisor(trim restart/callback)

## DOCS
docs/how-to/heterogeneous_topology.md → DELETE (mkdocs.yml nav)
docs/how-to/service_discovery.md → TRIM (keep peers/announce/shared_dir; drop nodes/ports)
docs/tutorials/parallel_tasks.md → TRIM to single-pool
docs/reference/parallel.md → TRIM API
examples/parallel_simple.py → keep (single pool)
CHANGELOG: add Unreleased entry
