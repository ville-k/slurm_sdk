[project]
name = "data-processing-tutorial"
version = "0.1.0"
description = "Data processing pipeline tutorial for SLURM SDK"
requires-python = ">=3.10"
dependencies = [
    "slurm-sdk>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
