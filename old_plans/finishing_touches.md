


- [ ] inject LoggerCallback by default
- [ ] argparse add a config file option?
- [ ] container packaging - convenient defaults:
    - if container specified, look for dockerfile based on conventions and defaults
    - default to push if containerfile specified
    - provide pre-canned containers with SDK installed
- [ ] LoggerCallbacks - configure logging on cluster too by default
- [ ] helpfull message for podman + cross builds if seeing the exect error


- [ ] print container size stats instead of the full output - only give full output on verbose or when theings fail